define( function ( require ) {
	'use strict';

	var baseController = require( 'apps/homepage/external/content/external/recommended/controllers/baseController' );

	describe ( 'baseControllerTest for Recommended', function () {

		it( 'should have `id` property with value of `recommended`', function () {
			// console.log( baseController );
		} );

	} );

} );